import { View } from "react-native";
import Card from "../../components/Card/Card";

export default function ResearchsPage() {
    console.log('View - 2');

    return <View>
        <Card height={100} />
        <Card height={100} />
    </View>
}